[phases.setup]
nixPkgs = ["python311", "gcc", "libffi"]

[phases.install]
cmds = [
  "pip install --upgrade pip",
  "pip install -r requirements.txt"
]

[phases.build]
cmds = [
  "mkdir -p harmonia_outputs",
  "mkdir -p data",
  "chmod +x start.sh"
]

[start]
cmd = "bash start.sh"
